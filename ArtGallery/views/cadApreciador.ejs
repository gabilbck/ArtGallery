<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastro de Artista</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link rel="stylesheet" href="/stylesheets/styleCadArt.css">
</head>
<body>
    <% if (!sucesso) { %>
    <div class="container">
        <h1 class="logo">ArtGallery</h1>
        <h2 class="titulo">Cadastrar Uma Nova Conta</h2>
        <p class="subtitulo">Insira as informacoes solicitadas e cadastre-se no ArtGallery</p>

        <% if (erros) { %>
        <div class="retorno">
            <div class="retorno-container" id="erro">
                <%= erros %>
            </div>
        </div>
        <% } %>

        <form action="/cadastro/apreciador" method="POST" class="form">
            <label for="email">E-mail</label>
            <input type="email" id="email" name="email" placeholder="email@dominio.com" required>

             <label for="nome">Nome completo</label>
            <input type="text" id="nome" name="nome" placeholder="Primeiro Nome" required>

            <label for="usuario">Nome de Usuário</label>
            <input type="text" id="usuario" name="usuario" placeholder="nome_de_usuario" required>

            <label for="senha">Senha</label>
            <input type="password" id="senha" name="senha" placeholder="Senha" required>

            <input type="hidden" name="tipo_usu" value="apr">

            <button type="submit" class="btn">Continuar</button> 
        </form>

        <p class="login-link">Ja possui uma conta? <a href="/login">Entre agora!</a></p>

        <a href="/" class="voltar">&#8592; Voltar</a>   
   </div>
   <% } else { %>
    <div class="retorno">
        <h1>Cadastro realizado com sucesso!</h1>
        <p>Aguarde enquanto redirecionamos você...</p>
        <div class="retorno-container" id="sucesso">
           Seu Cadastro foi realizado com sucesso!
        </div>
    </div>
    <script>
        setTimeout(() => { 
            window.location.href = "/login";
        }, 3001);
    </script>
    <% } %>
</body>
</html>